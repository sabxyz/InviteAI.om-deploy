# دليل ربط event.html بـ Google Sheet — خطوة بخطوة

هذا الدليل يشرح كيف تفعّل `event.html` (القالب الديناميكي) بحيث يقرأ بيانات كل زبون تلقائياً من Google Sheet، بدل إنشاء صفحة منفصلة يدوياً لكل زبون.

---

## 1) أنشئ Google Sheet جديد

افتح [sheets.google.com](https://sheets.google.com) → Sheet فارغ جديد → سمّه مثلاً `InviteAI - Events`.

بالتبويب الأول (Sheet1)، غيّر اسمه إلى **`Events`** بالضغط مرتين على اسم التبويب تحت.

بالصف الأول، اكتب هذي الأعمدة بالضبط (نفس الترتيب مو شرط، بس نفس الأسماء بالضبط):

| العمود | إلزامي؟ | الوصف | القيم المقبولة |
|---|---|---|---|
| `id` | ✅ إلزامي | معرّف فريد بالرابط — استخدم اسم صعب التخمين، مو أرقام متسلسلة | مثال: `sara-ahmad-2611` |
| `name` | ✅ إلزامي | اسم الزبون/المناسبة يظهر بالصفحة | نص حر |
| `event_type` | ✅ إلزامي | نوع المناسبة — يتحكم بالعنوان والنص الافتراضي | `wedding` `engagement` `graduation` `birthday` `national` `evening` `farewell` `other` |
| `date` | ✅ إلزامي | تاريخ ووقت المناسبة (يُستخدم للعد التنازلي) | صيغة ISO: `2026-09-20T18:00:00+04:00` |
| `location` | ✅ إلزامي | اسم المكان | نص حر |
| `location_url` | اختياري | رابط خرائط جوجل — لو موجود، اسم المكان يصير رابط قابل للضغط | رابط كامل |
| `theme` | ✅ إلزامي | الثيم المختار (اسمه بالإنجليزي، تلقاه بمعرض الثيمات themes.html بخانة `data-theme`) | `pulse` `glow` `bloom` `calm` `radiance` `twinkle` `leaves` |
| `package` | ✅ إلزامي | الباقة المدفوعة — تتحكم بإظهار/إخفاء الأقسام | `basic` `premium` `luxury` `custom` |
| `status` | ✅ إلزامي | حالة الصفحة | `pending` (لسا ما دفع عربون) · `preview` (نسخة معاينة بعلامة مائية) · `active` (نهائية بدون علامة مائية) |
| `phone` | اختياري | رقم جوال الزبون (للمتابعة الداخلية فقط — **لا يُرجّع أبداً بالرابط العام**) | نص/رقم |
| `note` | اختياري | نص ترحيبي مخصص يستبدل النص الافتراضي حسب نوع المناسبة | نص حر |
| `schedule` | اختياري (فاخرة فقط) | برنامج الحفل — يظهر فقط بالباقة الفاخرة | صيغة: `18:00\|استقبال الضيوف;19:00\|بدء الحفل;20:30\|العشاء` |
| `rsvp_count` | تلقائي — سيّبه فاضي | يتحدث تلقائياً من الكود لما ضيف يضغط "سأحضر" | رقم (يبدأ 0) |
| `dashboard_key` | اختياري (فاخرة فقط) | مفتاح سري عشوائي لحماية رابط لوحة متابعة المدعوين مستقبلاً | نص عشوائي طويل، مثال: `dk_92mF7xQ2` |

> 💡 نصيحة: أضف صف تجربة واحد بقيم وهمية (مثلاً `id = test-1`) عشان تختبر الصفحة قبل ما ترسل روابط حقيقية لزبائن.

---

## 2) أنشئ تبويب فارغ اسمه `RSVP_Log`

هذا التبويب يتعبى تلقائياً من الكود (لا تكتب فيه شي يدوي) — بس تأكد إنه موجود بنفس اسم `RSVP_Log` بالضبط. لو ما سويته يدوي، الكود بينشئه تلقائياً أول مرة يشتغل.

---

## 3) أضف الكود (Google Apps Script)

1. من داخل الشيت: **Extensions → Apps Script**
2. امسح أي كود موجود بملف `Code.gs` والصق محتوى ملف `Code.gs` المرفق معك كامل
3. احفظ (Ctrl+S / أيقونة الحفظ)

---

## 4) انشر الكود كـ Web App

1. زر **Deploy** (أعلى يمين) → **New deployment**
2. بخانة "Select type" اختر أيقونة الترس ⚙️ → **Web app**
3. الإعدادات:
   - **Execute as:** Me (حسابك)
   - **Who has access:** Anyone
4. اضغط **Deploy**
5. أول مرة، جوجل بيطلب صلاحية (Authorize access) — اختر حسابك واضغط Advanced → Go to (اسم المشروع) → Allow
6. بعد النشر، بيعطيك رابط شكله:
   `https://script.google.com/macros/s/AKfycb.../exec`
   **انسخه** — هذا رابط الـ API تبعك.

---

## 5) اربط الرابط بـ event.html

افتح `event.html` بأي محرر نصوص، ولاقي هذا السطر بأول السكربت:

```js
const CONFIG = {
  API_URL: "PASTE_YOUR_APPS_SCRIPT_WEB_APP_URL_HERE"
};
```

استبدل القيمة برابط الـ Web App اللي نسخته بالخطوة السابقة:

```js
const CONFIG = {
  API_URL: "https://script.google.com/macros/s/AKfycb.../exec"
};
```

احفظ الملف.

---

## 6) جرّب الصفحة

1. أضف صف تجربة بتبويب `Events` (مثلاً `id = test-1`, `status = preview`)
2. بعد ما ترفع `event.html` على Vercel (أو حتى تفتحه محلياً بالمتصفح)، افتح:
   `event.html?id=test-1`
3. المفروض تشوف الصفحة تتبنى تلقائياً بالثيم والبيانات اللي كتبتها، مع علامة مائية لأن الحالة `preview`.
4. غيّر `status` بالشيت لـ `active` وحدّث الصفحة → العلامة المائية المفروض تختفي.

---

## ملاحظات مهمة

- **الأمان:** الرابط العام (`event.html?id=...`) ما يرجّع رقم الجوال ولا عدد المؤكدين — بس البيانات اللي بالجدول `PUBLIC_FIELDS` بملف الكود. خلي أرقام الـ `id` صعبة التخمين (اسم + رقم عشوائي) عشان محد يفتح صفحة زبون ثاني بالغلط.
- **لوحة متابعة المدعوين (guest-dashboard.html):** حالياً نسخة تجريبية ثابتة. الكود (`Code.gs`) فيه دالة `?action=stats&key=...` جاهزة تربطها بها لاحقاً وتصير ديناميكية بنفس فكرة event.html — قول لي متى تبيها وأبنيها.
- **الباقة أساسية:** بحسب خطة التسعير، الأساسية تقتصر على ثيمين بسيطين. هذا القيد يُطبَّق يدوياً وقت تعبئة الشيت (لا تكتب بعمود `theme` إلا أحد الثيمين المسموحين لهذي الباقة) — الكود حالياً ما يمنع باقي الثيمات تقنياً.
- **مشاكل CORS:** لو ظهر خطأ اتصال بالمتصفح (Console)، تأكد إن "Who has access" بالنشر = Anyone، وإنك نشرت نسخة **جديدة** بعد أي تعديل بالكود (Deploy → Manage deployments → Edit → New version).
